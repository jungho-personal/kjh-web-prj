# 🛠️ Dev Log – 2025.01.03

## 📌 작업 요약
- Admin Markdown Editor 구현 완료
- JWT 기반 관리자 로그인 + 글 생성 기능 운영 배포 성공

---

## 🧩 작업 내용

### 1. Admin Editor UI 구현
- Tailwind v4 + shadcn/ui 기반 관리자 글쓰기 화면 구성
- Markdown 입력 영역 + 실시간 Preview 구성
- Published 상태 토글 및 메타 정보 입력 UI 구현

### 2. 관리자 인증(JWT) 구조 확정
- POST /api/login 엔드포인트 구현
- JWT 발급 시 role=admin 포함
- Admin API 접근 시 Authorization Bearer Token 검증

### 3. Admin 글 생성 API 연동
- POST /api/admin/posts 구현
- content_md 필드 기준 Markdown 본문 저장
- 저장 성공 시 slug 기반 상세 페이지로 이동

### 4. API Router Prefix 구조 정리
- /api prefix 중복(/api/api) 이슈 원인 파악
- main.py에서만 /api prefix 적용하도록 구조 통합
- Swagger 기준 엔드포인트 정상화

### 5. 프론트엔드 빌드 및 운영 배포 이슈 해결
- Vite 빌드 결과(static)와 Docker 이미지 간 불일치 문제 확인
- Docker 멀티 스테이지 빌드로 프론트 빌드 → backend/static 복사
- 운영 환경에서 SPA 라우팅(/admin/login) 정상 동작 확인

---

## ⚠️ 이슈 & 해결
- **이슈**: 운영 환경에서 /admin/login 화면이 표시되지 않음
  - **원인**: 프론트 빌드 산출물이 Docker 이미지에 포함되지 않음
  - **해결**: Docker 멀티 스테이지 빌드 적용하여 frontend 빌드 결과를 backend/static으로 복사

---

## ✅ 현재 상태
- 로컬 실행 여부: OK
- Docker 상태: OK
- 배포 상태: OK
- 외부 접근(URL): OK

---

## 🔜 다음 작업 (Next Step)
- [ ] Blog / Dev Log 리스트 페이지 UI 구현
- [ ] Admin Editor 글 수정(PATCH) 기능 추가
- [ ] LLM Playground 기능 연동

---

## 🏷️ Tags
`#FastAPI` `#React` `#JWT` `#AdminEditor` `#Docker` `#Railway` `#DevLog`

## 📂 Category
- Development Log
